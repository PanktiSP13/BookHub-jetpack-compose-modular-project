name: AI PR Review

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  ai-review-gemini:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get PR diff
        run: |
          git diff \
            origin/${{ github.event.pull_request.base.ref }}...HEAD \
            > pr.diff

      - name: Show diff size
        run: |
          echo "PR diff created"
          wc -l pr.diff

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Gemini SDK
        run: |
          pip install -U google-genai

      - name: Run AI Review (Gemini)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python scripts/ai_pr_review_gemini.py

      - name: Post Inline AI Review
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          python scripts/post_inline_review.py

#      - name: Post AI Review to PR
#        env:
#          GH_TOKEN: ${{ github.token }}
#          REVIEW_FILE: ai-review.json
#          PR_NUMBER: ${{ github.event.pull_request.number }}
#          REPOSITORY: ${{ github.repository }}
#        run: |
#          python - <<'PY'
#          import json
#          import os
#          import urllib.request
#
#          with open("ai-review.json", "r", encoding="utf-8") as f:
#              review = json.load(f)
#
#          findings = review.get("findings", [])
#
#          if not findings:
#              body = """## 🤖 AI PR Review
#
#          **No actionable issues found.**
#
#          The AI reviewer did not identify any significant issues in the changed code.
#          """
#          else:
#              body = "## 🤖 AI PR Review\n\n"
#
#              for finding in findings:
#                  severity = finding["severity"]
#                  file = finding["file"]
#                  line = finding["line"]
#                  category = finding["category"]
#                  problem = finding["problem"]
#                  why = finding["why_it_matters"]
#                  fix = finding["suggested_fix"]
#
#                  icon = {
#                      "CRITICAL": "🚨",
#                      "HIGH": "🔴",
#                      "MEDIUM": "🟡",
#                      "LOW": "🔵"
#                  }.get(severity, "⚪")
#
#                  body += f"""### {icon} {severity} — {category}
#
#          **File:** `{file}`"""
#
#                  if line and line != "unknown":
#                      body += f"  \n**Line:** `{line}`"
#
#                  body += f"""
#
#          **Problem:**
#          {problem}
#
#          **Why it matters:**
#          {why}
#
#          **Suggested fix:**
#          {fix}
#
#          ---
#          """
#
#          body += "\n_Generated automatically by Gemini AI PR Review._"
#
#          url = (
#              f"https://api.github.com/repos/"
#              f"{os.environ['REPOSITORY']}/issues/"
#              f"{os.environ['PR_NUMBER']}/comments"
#          )
#
#          data = json.dumps({"body": body}).encode("utf-8")
#
#          request = urllib.request.Request(
#              url,
#              data=data,
#              method="POST",
#              headers={
#                  "Authorization": f"Bearer {os.environ['GH_TOKEN']}",
#                  "Accept": "application/vnd.github+json",
#                  "X-GitHub-Api-Version": "2026-03-10",
#                  "Content-Type": "application/json"
#              }
#          )
#
#          with urllib.request.urlopen(request) as response:
#              print("GitHub comment created:", response.status)
#          PY